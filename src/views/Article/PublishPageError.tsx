import * as React from "react";

export const PublishPageError: React.FC = props => {
    return (
        <div>
            <p className="publish-page-heading">Please publish the page to continue.</p>
        </div>
    );
}
export default PublishPageError;
